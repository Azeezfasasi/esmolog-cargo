// Central API base for the Next.js app.
// Since your Next API routes live under `src/app/api/*`, calls should be same-origin.
// If you later deploy a separate backend, set this to the backend origin (e.g. https://api.example.com).

export const API_BASE_URL = '/api';


